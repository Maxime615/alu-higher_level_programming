| `1-last_digit.py` | Assigns a random signed number to a variable and prints its last digit with a conditional message. |
| `2-print_alphabet.py` | Prints the ASCII alphabet in lowercase, not followed by a new line, using only one loop and one `print` function. |
| `3-print_alphabt.py` | Prints the ASCII alphabet in lowercase except for the letters `q` and `e`. |
| `4-print_hexa.py` | Prints all numbers from `0` to `98` in both decimal and hexadecimal formats. |
| `5-print_comb2.py` | Prints numbers from `00` to `99` separated by a comma and a space, in ascending order. |
| `6-print_comb3.py` | Prints all possible different combinations of two digits in ascending order (e.g., `01`, `02`... `89`). |
| `7-islower.py` | A function `def islower(c):` that checks for lowercase characters. Returns `True` or `False`. |
| `8-uppercase.py` | A function `def uppercase(str):` that prints a string in uppercase followed by a new line. |
| `9-print_last_digit.py` | A function `def print_last_digit(number):` that prints and returns the last digit of a number. |
| `10-add.py` | A function `def add(a, b):` that adds two integers and returns the result. |
| `11-pow.py` | A function `def pow(a, b):` that computes $a^b$ (a to the power of b) and returns the value. |
| `12-fizzbuzz.py` | A function `def fizzbuzz():` that prints numbers from `1` to `100`. Prints `Fizz` for multiples of 3, `Buzz` for multiples of 5, and `FizzBuzz` for both. |
